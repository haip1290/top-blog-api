const generalMsg = {
  isRequire: "is required",
  isPositiveInt: "must be a positive integer",
  isAlphanumeric: "must be alphabet letters and numbers",
};

const usernameMsg = { length: "must be in range 3-20 characters" };

const passwordMsg = { length: "must be in range 6-20 characters" };

const emailMsg = { valid: "Please enter a valid email address" };

export { generalMsg, usernameMsg, passwordMsg, emailMsg };
